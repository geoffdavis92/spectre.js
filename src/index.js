// index @flow

import { select, selectAll } from './functions'
import Tabs from './components/Tabs'

const Spectre = {
	select,
	selectAll,
	Tabs
}

export default Spectre

export {
	select,
	selectAll,
	Tabs
}